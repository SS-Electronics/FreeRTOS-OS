/*
 * Forwarding header — include the standalone FSM library from anywhere
 * in the FreeRTOS-OS project:
 *
 *   #include <lib/fsm.h>
 *
 * The library itself (lib/fsm/) has no OS dependency and can also be
 * copied out and used as a standalone release.
 */
#include "../../lib/fsm/fsm.h"
