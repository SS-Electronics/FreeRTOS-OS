# File:        log/Makefile
# Author:      Subhajit Roy
#
# Module:      log
# Info:        Structured severity logger (slog) — ring buffer, severity levels,
#              timestamp, printk echo for ERROR/WARN.
#
# This file is part of FreeRTOS-OS Project.

include autoconf.mk

# ── Structured logger ──────────────────────────────────────────────────────
obj-y += log/slog.o
